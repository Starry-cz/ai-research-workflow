# 贡献指南

感谢你帮助维护这份面向零基础学习者的 AI 科研工作流工具库。

本项目优先收录能解决明确科研问题、具有可核验来源、能够产生实际学习或研究产物的资源。Star 高、传播广或“功能很多”不能单独构成收录理由。

## 贡献类型

- 修复失效链接、错误描述或过期信息；
- 新增适合零基础或科研实践的课程、工具、模板和指南；
- 改进学习路线、复现流程、实验检查或学术诚信说明；
- 补充某个资源的前置能力、建议产物和重要限制；
- 增加能够被他人直接复用的中文模板。

## 新增资源需要提供

1. 工具或项目名称；
2. 原始仓库、官方文档或正式服务链接；
3. 适用阶段；
4. 建议起点：L0、L1、L2 或 L3；
5. 前置能力；
6. 一句话定位；
7. 学习或使用后应留下的具体产物；
8. 已知限制：覆盖范围、维护状态、登录、费用、数据来源、许可证或兼容性；
9. 最近一次核验日期。

## 能力等级

| 等级 | 定义 |
| --- | --- |
| L0 | 不熟悉终端、Git、Python 环境或 Jupyter |
| L1 | 能运行样例，但不能完整解释训练、评测和数据划分 |
| L2 | 能复现 baseline、定位差异并保存实验记录 |
| L3 | 能提出可证伪假设并完成对照、消融和失败分析 |

## tools.yml 条目示例

```yaml
- id: example-project
  name: Example Project
  name_zh: 示例项目
  stage: [论文阅读, 代码复现]
  level: [L1, L2]
  type: github-course
  url: https://github.com/example/example-project
  summary: 用一句话说明它解决什么问题。
  recommended_output: 完成后应留下的 Notebook、代码、实验卡或报告。
  star_snapshot: "约 1 万（YYYY-MM-DD）"
  caveat: 至少写明一个重要限制。
```

Star 是动态信息，只能作为社区可见度快照，不能决定排序或推荐等级。确需记录 Star 时必须写明核验日期，并避免使用“排名第一”“最好”等不可证实表述；面向新手的入口优先按任务、能力阶段和可验收产物组织。

## 修改原则

- 只写能够从官方仓库、官方文档、论文原文或正式规则核验的信息；
- 不复制外部项目的大段 README、代码、图片或数据；
- 资源说明应转换为本仓库自己的学习产物和使用边界；
- 不把个人体验写成普遍结论；
- 不要求零基础学习者在开始实践前完成不必要的完整课程体系；
- 聚合平台用于发现，论文、代码、录用状态和规则必须回到原始来源；
- 会议 deadline、模板、匿名、伦理和 AI 政策以目标 venue 最新官方说明为准；
- 工具失效时直接修正或标记待核验，不保留未经确认的断言。
- 会改变读者入口、工作流、模板字段或维护方式的修改，应同步更新 `CHANGELOG.md`。

## 模板贡献要求

新增模板应满足：

- 有明确使用阶段和完成标准；
- 字段能够帮助复现、审计或决策；
- 不绑定某个模型、平台或商业服务；
- 不鼓励编造缺失信息；
- 缺失信息应进入待核验清单或风险登记表；
- 尽量保持一页到两页，避免成为无法执行的长表格。
- 说明模板属于哪个最小阶段包或由什么事件触发；
- 说明为什么现有模板不能覆盖该需求，避免同义字段和重复台账持续增长；
- 新模板加入后重新检查 L0–L3 的最短路径，不得默认要求新手一次填写全部模板。

## 示例贡献要求

- 示例必须明确区分官方信息、实际运行观察、作者假设和待核验项；
- 不编造命令输出、指标、运行时间或实验成功状态，示例数值必须能回到仓库内脚本、配置和日志；
- 教学演练必须声明其不能替代论文复现、真实数据实验或目标领域的统计规范；
- 涉及方法比较时，应预先写明运行集合、调参预算、模型选择、停止和排除规则，不展示“挑出的最好一次”代替完整结果；
- 外部经验帖可以帮助识别问题，但技术要求应同时由官方文档、原项目或正式规范核验。

## README 排版要求

- 首屏应让新读者快速确认“仓库解决什么问题、适合谁、从哪里开始”，并提供按能力或任务选择的直接入口；
- 主 README 只保留定位、选择入口、阶段地图和资源导航；超过一个屏幕的执行协议、领域细节或故障说明应进入 `docs/`，并从对应阶段直接链接；
- 下沉内容不能只保留“查看更多”：首页必须同时写出下一项行动、最低产物或通过条件，让读者不打开所有文档也能选择路径；
- 主 README 的表格默认不超过两列；确需多维比较时，拆成多个两列表格或改用分组卡片；
- 不设置仅包含“周次、等级、Star、阶段”等短值的独立窄列，应与相邻说明合并；
- 单元格中有两类信息时，使用短标签和 `<br>` 分行，例如“**产物**”与“**完成**”；
- 不依赖自定义 CSS、固定像素宽度或只在桌面端有效的 HTML 样式；
- 修改 README 后至少检查约 375 px 手机宽度和常规桌面宽度，确认没有逐字换行、正文溢出或必须横向滚动才能理解的核心内容。

## 提交前检查

- [ ] 链接可以打开，并指向原始项目或官方入口
- [ ] 已注明适用等级、前置能力和建议产物
- [ ] 描述没有夸大承诺，且至少说明一个重要限制
- [ ] 动态信息包含核验日期
- [ ] README.md 与 tools.yml 中的名称、链接和定位一致
- [ ] 新增 Markdown 的标题、表格、相对链接和代码块可以正常渲染
- [ ] 主 README 的表格不超过两列，且已检查手机窄屏排版
- [ ] 新读者可以在一分钟内按当前能力找到起点和第一个可验证产物
- [ ] 同一资源没有在首页的多个导航体系中重复出现，动态 Star 不承担学习排序功能
- [ ] tools.yml 可以被 YAML 解析器读取
- [ ] 没有提交密钥、个人数据、未公开论文或受版权保护的外部文件
- [ ] 问题反馈包含具体位置、预期行为、实际问题和可核验依据
- [ ] 面向读者的行为变化已经记录到 `CHANGELOG.md`
